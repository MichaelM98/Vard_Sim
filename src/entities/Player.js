// Player entity: position, HP, prayer state, and movement resolution.
// TODO: implement tile-by-tile movement toward a clicked destination.

export function movePlayerTowards(player, targetTile) {
  // placeholder — filled in when we build click-to-move
}
